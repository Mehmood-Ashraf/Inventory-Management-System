const useProducts = () => {

    const productDetailsHandler = (productId) => {
        try {
            
        } catch (error) {
            
        }
    }

    return {productDetailsHandler}
}

export default useProducts;